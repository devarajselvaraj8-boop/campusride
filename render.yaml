services:
  - type: web
    name: campusride-notification-server
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DEV_TEST_SECRET
        generateValue: true

